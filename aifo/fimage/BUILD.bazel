load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "fimage",
    srcs = glob(["src/**/*.cpp"]),
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
    deps = [
        "//third_party/lodepng",
        "@libtiff",
    ],
)
