# SlideScope Sample Configuration File
# Copy this file to config.yaml and modify as needed
server:
  port: "3000"
  host: "localhost"
  base_path: "" # Set to "/slidescope" if serving under a subdirectory
  environment: "development" # Options: development, production
  static_assets_dir: "./dist" # Directory with static web assets
  cors:
    enabled: true
    allow_origins:
      - "*"
    allow_methods:
      - "GET"
      - "POST"
      - "PUT"
      - "DELETE"
      - "OPTIONS"
    allow_headers:
      - "Origin"
      - "Content-Type"
      - "Accept"
      - "Authorization"
    expose_headers: []
    allow_credentials: true
    max_age: 86400
database:
  url: "sqlite:///slidescope.db"
auth:
  jwt_secret: "change-this-in-production" # JWT signing secret
  jwt_algorithm: "HS256" # JWT signing algorithm
  jwtExpirationMinutes: 5 # 5 minutes for the JWT token
  jwtRefreshExpirationMinutes: 30 # 30 minutes for the refresh token
  cookie:
    name: "slidescope_token"
    path: "/"
    http_only: true
    secure: false # Set to true for HTTPS
    same_site: "lax"
storage:
  slides_dir: "./data/slides" # Directory for slide storage
  masks_dir: "./data/masks" # Directory for mask storage
  cache_size: 100 # Number of slides to keep in memory cache
logging:
  level: "info" # Options: debug, info, warn, error 
