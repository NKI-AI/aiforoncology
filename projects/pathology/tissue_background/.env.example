# The root where to save the logs
PROJECT_ROOT="/home/user/experiments/tissue_background/"
# The root where the annotations are located
ANNOTATIONS_DIR=/data/annotations/
DATA_DIR=/scratch/tissue_background_dataset/
# The location where the database is located
DATABASE_URI=sqlite:////data/annotations/manifest.sqlite 