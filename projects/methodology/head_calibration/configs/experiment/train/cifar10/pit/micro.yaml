# @package _global_
defaults:
  - override /data: cifar
  - override /model: pit/micro
tags: ["cifar10", "pit"]
seed: null
trainer:
  min_epochs: 10
  max_epochs: 50
model:
  net:
    num_channels: 3
    img_dim: 32
data:
  batch_size: 256
experiment_name: "train-cifar10_pit"
run_label: "micro"
