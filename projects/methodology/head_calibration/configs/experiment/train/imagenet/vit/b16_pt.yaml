# @package _global_
defaults:
  - override /data: imagenet
  - override /model: vit/b16_pt
tags: ["imagenet", "vit"]
seed: null
trainer:
  min_epochs: 10
  max_epochs: 50
model:
  num_classes: 1000
  net:
    num_channels: 3
    num_classes: ${model.num_classes}
data:
  batch_size: 128
  img_size: 224
experiment_name: "train-imagenet_vit"
run_label: "b16_pt"
