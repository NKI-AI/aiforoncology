# @package _global_
defaults:
  - override /data: imagenet
  - override /model: alexnet/alexnet
tags: ["imagenet", "alexnet"]
seed: null
trainer:
  min_epochs: 10
  max_epochs: 50
model:
  num_classes: 1000
  net:
    num_channels: 3
    num_classes: ${model.num_classes}
data:
  batch_size: 4096
experiment_name: "train-imagenet_alexnet"
run_label: "alexnet"
