# @package _global_
defaults:
  - override /data: mnist
  - override /model: resnet/resnet34
tags: ["mnist", "resnet"]
seed: null
trainer:
  min_epochs: 10
  max_epochs: 50
model:
  net:
    num_channels: 1
data:
  batch_size: 2048
experiment_name: "train-mnist_resnet"
run_label: "r34"
