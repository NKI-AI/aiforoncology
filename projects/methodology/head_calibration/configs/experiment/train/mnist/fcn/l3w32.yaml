# @package _global_
defaults:
  - override /data: mnist
  - override /model: fcn/l3_w32
tags: ["mnist", "fcn"]
seed: null
trainer:
  min_epochs: 10
  max_epochs: 50
model:
  net:
    num_channels: 1
    img_dim: 28
data:
  batch_size: 2048
experiment_name: "train-mnist_fcn"
run_label: "l3_w32"
