# @package _global_
defaults:
  - override /data: cifar
  - override /model: simpleconv/l2_f16
tags: ["cifar100", "simpleconv"]
seed: null
trainer:
  min_epochs: 10
  max_epochs: 50
data:
  batch_size: 2048
  cifar_version: 100
model:
  num_classes: 100
  net:
    num_classes: 100
experiment_name: "train-cifar100_simpleconv"
run_label: "l2_f16"
