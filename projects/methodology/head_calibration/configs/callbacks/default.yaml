defaults:
  - model_checkpoint
  - model_summary
  # - rich_progress_bar
  # - cal_metrics
  # - difficult_samples
  - _self_
model_checkpoint:
  dirpath: ${paths.output_dir}/checkpoints
  filename: "epoch_{epoch:03d}"
model_summary:
  max_depth: -1
